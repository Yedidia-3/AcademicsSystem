
  # Prototype Development

  This is a code bundle for Prototype Development. The original project is available at https://www.figma.com/design/1Vpwdz6pkSSqifsecDcfpy/Prototype-Development.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  