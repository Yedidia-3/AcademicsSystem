{
  "buildCommand": "npm --prefix apps/frontend install && npm --prefix apps/frontend run build",
  "outputDirectory": "apps/frontend/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
